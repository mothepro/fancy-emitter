{
  "name": "fancy-emitter",
  "author": "Maurice Prosper",
  "description": "A new take on JavaScript's EventEmitter class. Makes use of types and the newest JS features.",
  "keywords": [
    "event",
    "emitter",
    "eventemitter",
    "strict"
  ],
  "version": "1.1.0",
  "scripts": {
    "build:npm": "tsc",
    "build:bundle": "npm run build:bundle:dev && npm run build:bundle:prod",
    "build:bundle:dev": "simplifyify index.ts -s emitter -o dist/umd/index.js --debug --bundle",
    "build:bundle:prod": "terser dist/umd/index.js -cmo dist/umd/index.min.js",
    "build:esm": "tsc -t esnext -m es2015 --outDir dist/esm index",
    "postbuild:esm": "tsc-esm dist/esm/index.js dist/esm/**/*.js",
    "build": "npm run build:npm && npm run build:bundle && npm run build:esm",
    "test": "mocha -r ts-node/register test/*.ts",
    "prerelease": "npm run build",
    "release": "np"
  },
  "main": "dist/index.js",
  "module": "dist/esm/index.js",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/mocha": "^5.2.6",
    "@types/should": "^13.0.0",
    "esm": "^3.2.25",
    "mocha": "^6.2.2",
    "np": "^5.0.1",
    "should": "^13.2.3",
    "simplifyify": "^7.0.2",
    "terser": "^3.17.0",
    "ts-node": "^8.4.1",
    "tsc-esm": "^1.0.4",
    "typescript": "^3.6.4"
  }
}
